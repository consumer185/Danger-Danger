// on click/tap, select a random number between 1 and 21 inclusive, use this to select an image *.svg to display in a random position, rotation, vertical

window.addEventListener('touchstart', function() {
  alert("the user touched the screen!!");
});
